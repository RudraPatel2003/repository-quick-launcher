<UserControl
  x:Class="Flow.Launcher.Plugin.RepositoryQuickLauncher.UI.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.modernwpf.com/2019"
  xmlns:vm="clr-namespace:Flow.Launcher.Plugin.RepositoryQuickLauncher.UI"
  d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
  d:DesignHeight="350"
  d:DesignWidth="450"
  mc:Ignorable="d"
>
  <Grid Margin="{StaticResource SettingPanelMargin}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ContentControl x:Name="CommandSettingsPanelHost" Grid.Row="0" />

    <Separator Grid.Row="1" Style="{StaticResource SettingPanelSeparatorStyle}" />

    <Grid Grid.Row="2">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <ListView
        x:Name="WindowsDirectoriesList"
        Grid.Row="0"
        Margin="{StaticResource SettingPanelItemTopBottomMargin}"
        BorderBrush="DarkGray"
        BorderThickness="1"
        ItemsSource="{Binding WindowsDirectoriesCollection}"
        SelectionMode="Extended"
      >
        <ListView.View>
          <GridView>
            <GridViewColumn Width="Auto" Header="Windows Directories" />
          </GridView>
        </ListView.View>
      </ListView>

      <DockPanel Grid.Row="1">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button
            MinWidth="100"
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Click="WindowsDirectoriesButtonAddClick"
            Content="Add"
          />
          <Button
            MinWidth="100"
            Margin="{StaticResource SettingPanelItemTopBottomMargin}"
            Click="WindowsDirectoriesButtonDeleteClick"
            Content="Delete"
          />
        </StackPanel>

        <TextBox
          x:Name="WindowsDirectoriesInput"
          Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
        />
      </DockPanel>
    </Grid>

    <Separator Grid.Row="3" Style="{StaticResource SettingPanelSeparatorStyle}" />

    <Grid Grid.Row="4">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <ListView
        x:Name="WslDirectoriesList"
        Grid.Row="0"
        Margin="{StaticResource SettingPanelItemTopBottomMargin}"
        BorderBrush="DarkGray"
        BorderThickness="1"
        ItemsSource="{Binding WslDirectoriesCollection}"
        SelectionMode="Extended"
      >
        <ListView.View>
          <GridView>
            <GridViewColumn Width="Auto" Header="WSL Directories" />
          </GridView>
        </ListView.View>
      </ListView>

      <DockPanel Grid.Row="1">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button
            MinWidth="100"
            Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
            Click="WslDirectoriesButtonAddClick"
            Content="Add"
          />
          <Button
            MinWidth="100"
            Margin="{StaticResource SettingPanelItemTopBottomMargin}"
            Click="WslDirectoriesButtonDeleteClick"
            Content="Delete"
          />
        </StackPanel>

        <TextBox
          x:Name="WslDirectoriesInput"
          Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
        />
      </DockPanel>
    </Grid>
  </Grid>
</UserControl>
